# セッション備忘録（2026/07/14）

前回（20260713）＝order-report-sender-info 実装完了・コミット済み（MaterialModule `ab73774`／Nonaka `564aa61`）、申請ボタン左寄せ確認OK。印刷（PDF出力）方式の協議を開始（未確定・spec未着手）。本日は印刷方式の要件を1項目ずつ確定していく。

> 印刷方式協議の全体像・技術評価（ブラウザはサイレント不可／PrintAgent 実挙動／CommonModule 公開状況）は **20260713 の「次テーマ：印刷（PDF出力）方式の協議メモ」** を参照。本ファイルは 7/14 の確定・新規論点を積み上げる。

## 用語定義（本日確定・以降統一）
- **PDFプリント**：ローカルPCの**既定プリンタ**へ、**印刷ダイアログを表示**して出力する方式（ブラウザ経路。旧 (B)/B-1）。Web(MainWeb)が PDF を**HTTPでブラウザへ配信**し、ユーザーがダイアログで印刷。ローカルPCは共有へ直接アクセス不要。
- **PDFエージェント**：サーバサイドの**サイレントエージェント（PrintAgent＋SumatraPDF）**でPDF出力する方式（旧 (A)）。`t_print_queue` に printer_name 指定投入 → 無確認印刷。

## 本日の確定事項
1. **PDF保管パスは固定** `\\OJIADM23120073\app_share\PrintAgent\`（全帳票PDFを単一保管・保持）。
2. **PDFプリントでも固定パスで問題なし**（確認済）。条件＝**ブラウザにはUNCパスを渡さず、Webが HTTP でPDFバイトを配信**する（`file://\\...` リンクを渡さない）。
   - PDFエージェント：固定共有から**エージェントが読取**（共有必須）。
   - PDFプリント：固定パスに保管しつつ **Web が HTTP 配信**（保存済み再利用時は **MainWeb に共有読取権限**が必要／都度生成ストリームなら権限不要）。
   - ＝**固定パスは両方式で共用（保管の単一化）**。

## 新規オープン論点（次に詰める）
- **判別管理**：**ページ単位**および**ユーザー単位（User / System）**で「PDFプリント／PDFエージェント」のどちらで出力するかを、どう管理・保持するか。
  - 例の観点：帳票（ページ）ごとの既定方式、ユーザー個別の上書き（User）、システム既定（System）の優先順位。設定テーブルの持ち方（MaterialModule 側）。
  - 20260713 の宿題②（(A)割当単位＝帳票/部署）・⑥（割当マスタ所属＝MaterialModule）とも関連。

## 20260713 からの継続オープン論点（再掲・未確定）
- ①(B=PDFプリント) の方式は B-1（ブラウザ・ダイアログあり・登録不要）で確定の見込み（用語化により (B)=PDFプリント=ブラウザ経路に整理）。※B-2 サイレント個人登録は不採用方向。
- ②(A=PDFエージェント) の割当単位：帳票ごと／部署（送付先/工場）ごと。
- ③Dispatches 2カ所出力＝PDFエージェント(Printer_D)＋PDFプリント(手元既定)。
- ④未到達フォールバック：サーバ既定へ(要 PrintAgent 改修) か エラー通知留め か。
- ⑤CommonModule への read I/F 追加可否（`IPrinterQueryService`：m_printer 読取公開）。PDFエージェント用途に限定。
- ⑦SMTP送信テスト機能の扱い（本テーマ統合 or 別 spec）。

## 対象帳票（20260713 提示表）
- PDFプリント（ローカル既定）：Orders/Create=発注書兼納入仕様書／Receivings=入庫伝票／Dispatches=原材料工場入請求。
- PDFエージェント（サーバ登録）：Dispatches=原材料工場入請求→Printer_D。
- Orders/Create 適用＝OutputType（出力区分）に準ずる。

## 再開合図
「再開します、session-memoを確認」。最新は本ファイル（20260714）。次アクション＝「ページ単位／ユーザー単位（User/System）での PDFプリント・PDFエージェント 判別の管理方法」を詰める → 続けて ②〜⑤・⑦ を確定 → CommonModule（一覧I/F）→ MaterialModule（判別・A/B・2カ所出力）の順で spec 起票。

---

## 追加要件：ユーザーサイドのテスト出力（本日）

- 現状：**テスト出力（テスト印刷・テストSMTP送信）は CommonModule のシステム側で確認可能**（`m_send_config` の TestFaxNumber/TestEmail、`docs/smtp-sender実送信テスト手順.md` 等）。
- 追加要望：**ユーザーサイドでも「テスト印刷」「テストSMTP送信」を実行できるようにしたい**。
  - テスト印刷＝PDFプリント／PDFエージェントの疎通を、ユーザー自身が画面から実行して確認できる。
  - テストSMTP送信＝ユーザー自身が画面からテストメール送信して疎通確認できる。
- 論点（次に詰める）：
  - 実行UIの配置（MaterialModule のどのページ／共通のユーザー向けページ）。
  - テスト印刷の対象＝「自分のPDFプリント（ローカル既定）」と「割当済み PDFエージェント（登録プリンタ）」の両方を試せるか。
  - テストSMTP送信の宛先＝ユーザー自身のメール（ApplicationUser.Email／m_general_personal_info.email）を既定にするか、入力にするか。誤送信防止（本番宛先へ送らない）。
  - 権限：一般ユーザーに許可する範囲（自分宛・自分の既定プリンタのみ等）。
- → 20260713 宿題⑦「SMTP送信テスト機能」はこの**ユーザーサイド テスト出力**として本テーマに統合する方向。

## 再開合図（更新）
「再開します、session-memoを確認」。最新は本ファイル（20260714）。次アクション＝(1)ページ単位/ユーザー単位(User/System)での PDFプリント・PDFエージェント 判別の管理方法、(2)ユーザーサイド テスト印刷/テストSMTP送信 の要件、を詰める → ②〜⑤ 確定 → CommonModule（一覧I/F・必要ならテスト送信の再利用）→ MaterialModule（判別・A/B・2カ所出力・テスト出力UI）で spec 起票。

---

## 現時点の要件（ユーザー提示表・2026/07/14）

列＝ページ／PDF生成(帳票)／使用者／出力先／出力タイミング／出力方式／制御主体／出力条件／テスト出力。方式語彙＝**PDFプリント**（ローカル既定・ダイアログ）／**PDFエージェント**（サーバ登録プリンタ・サイレント）／**SMTPエージェント**（メール/FAX送信・既存 DispatchEnqueue→SmtpQueue）。

### 読み取り（暫定・※は要確認）
- **Orders/Create（発注書兼納入仕様書）**／使用者=現場／タイミング=**Approvals 承認後**／制御主体=ユーザー設定。
  - 出力条件＝**出力区分(OutputType)で判定**：0=出力なし(PDF生成なし)／1=印刷／2=FAX(SMTPエージェント)／3=印刷+FAX。
  - ※Q1：行の「出力先=ローカル既定／方式=PDFエージェント」と、出力条件「1=印刷(PDFプリント)」が食い違い。**1=印刷 の実体は PDFプリント か PDFエージェントか要確認**。
  - テスト出力＝「テスト出力」チェックボックス。**ON時は Approvals を経由せず即時出力**。SMTP側はテスト時**送信先=ユーザー自身のメール**。※出力要件に準ずる。
- **Dispatches（原材料工場入請求）**／現場／タイミング=**「請求」押下時**。2系統:
  - (i) ローカル既定へ **PDFプリント**（制御=ユーザー設定・現行：PDFチェックボックスON で出力）。
  - (ii) 神崎工場○○プリンタ（**サーバサイド登録**）へ（制御=**システム設定**・新規：システム側で出力プリンタを設定、出力有無は設定しない＝常時出力）。※Q2：方式は **PDFエージェント** で確定か（表記ゆれ確認）。
- **Receivings（入庫伝票）**／現場／タイミング=**「入庫伝票」押下時**／ローカル既定へ **PDFプリント**（制御=ユーザー設定）。※Q3：出力条件欄「新規：システム側で出力プリンタ設定…」は Dispatches(ii) との取り違えでは？ Receivings はユーザー設定の PDFプリントで良いか。

### この表から見えた設計モデル（重要）
- **制御主体 = ユーザー設定 / システム設定** の2値。＝「判別管理」の答えの一部：**ページ×出力先ごとに、制御をユーザーが持つ(ユーザー設定)かシステム固定(システム設定)か**を定める。
  - ユーザー設定：そのユーザーの出力先/有無（例 ローカル既定への PDFプリント可否）を本人が設定。
  - システム設定：サーバ登録プリンタ等、システム側で固定（ユーザーは変更不可・出力有無も固定）。
- **テスト出力 = チェックボックス方式**：本番の承認/送信フロー（Approvals 等）を**バイパス**して即時に、印刷はそのまま／SMTPは**自分宛**に送る。＝前述「ユーザーサイド テスト出力」の具体像。
- 出力タイミングはページで異なる：Orders/Create=承認後、Dispatches=「請求」押下、Receivings=「入庫伝票」押下。

### 要確認（次に潰す）
- Q1：Orders/Create「1=印刷」＝PDFプリント／PDFエージェントどちら？（承認後の印刷は誰の・どこのプリンタへ？）
- Q2：Dispatches(ii) サーバ登録プリンタ＝PDFエージェント（システム設定・常時出力）で確定か。
- Q3：Receivings の出力条件（ユーザー設定 PDFプリントで良いか／システム設定の混入は誤記か）。
- Q4：「ユーザー設定/システム設定」の粒度＝ページ×出力先単位で持つ、で良いか。User/System の優先順位は？
- Q5：テスト出力チェックボックスの搭載ページ（Orders/Create のみ／Dispatches・Receivings にも）と、印刷テストの対象（PDFプリント／PDFエージェント どちらを試せるか）。

### 再開合図（更新）
「再開します、session-memoを確認」。最新は本ファイル（20260714）。次アクション＝上表の Q1〜Q5 を確定 → 判別管理（ユーザー設定/システム設定）とテスト出力（チェックボックス）を軸に要件確定 → CommonModule（プリンタ一覧I/F 等）→ MaterialModule（判別・PDFプリント/PDFエージェント/SMTPエージェント・2カ所出力・テスト出力）で spec 起票。

---

## 回答反映（Q1〜Q3＋テスト出力・2026/07/14）

- **Q1（Orders/Create「1=印刷」）＝PDFエージェント**。かつ**ユーザー毎に出力プリンタを設定可能**にする（各ユーザーが自分の PDFエージェント出力プリンタを設定）。**前提：サーバサイドでのプリンタエントリ（登録）が必要**（登録済みプリンタの中からユーザーが選択）。
- **Q2（Dispatches(ii) サーバ登録プリンタ）**：**システムサイドで「外部出力」がON の場合に出力**（常時ではない）。＝システム設定の「外部出力」フラグで出力有無を制御。
- **Q3（Receivings）**：**ユーザー設定の PDFプリント**で確定。
- **テスト出力チェックボックス**：**エージェント（PDFエージェント／SMTPエージェント）を利用する場合のみ必要**。PDFプリント（ローカル・ダイアログ）には不要。

### 確定した方式マトリクス（現時点）
| ページ | タイミング | 方式 | 制御 | 出力先 | テスト出力CB |
|---|---|---|---|---|---|
| Orders/Create 発注書 | Approvals承認後 | 出力区分で判定：1=印刷→**PDFエージェント**（ユーザー毎に出力プリンタ設定）／2=FAX→**SMTPエージェント**／3=両方／0=なし | ユーザー設定 | ユーザー割当のサーバ登録プリンタ／仕入先(FAX/メール) | 要（エージェント） |
| Dispatches 原材料工場入請求 (i) | 「請求」押下 | **PDFプリント** | ユーザー設定 | ローカル既定 | 不要 |
| Dispatches 原材料工場入請求 (ii) | 「請求」押下 | **PDFエージェント** | **システム設定「外部出力」ON時のみ** | サーバ登録プリンタ（例 神崎工場） | 要（エージェント） |
| Receivings 入庫伝票 | 「入庫伝票」押下 | **PDFプリント** | ユーザー設定 | ローカル既定 | 不要 |

### 設計モデル（更新）
- **PDFエージェント出力プリンタ＝ユーザー毎の割当**（各ユーザー → サーバ登録プリンタ名）。割当マスタは MaterialModule 側（例 `m_user_print_setting`：user_code, printer_name 等）。**選択元のサーバ登録プリンタ一覧は CommonModule から取得**（＝read I/F 追加が必要＝旧⑤が確定的に必要）。
- **サーバサイドのプリンタ登録（agentへのエントリ）**＝システム/管理者側（別途）。ユーザーは登録済みから**選ぶ**だけ。
- **「外部出力」フラグ（システム設定）**＝Dispatches(ii) の PDFエージェント出力有無を制御。保持場所＝要確認（MaterialModule のシステム設定 or CommonModule）。
- **テスト出力**＝エージェント利用時のみ。CB ON で承認/本番フローをバイパスし、PDFエージェント＝自分の割当プリンタへテスト、SMTPエージェント＝自分宛メールへテスト。

### 残確認
- R1：**CommonModule プリンタ一覧 read I/F の追加**を許可するか（ユーザー割当の選択元に必須）。不可なら MaterialModule から db_common `m_printer` 読み取り専用参照（インターフェース経由でないトレードオフ）。
- R2：**PDFエージェント出力プリンタのユーザー割当UI**は、ユーザー自身が設定（自己サービス）か、管理者が代理設定か。
- R3：**「外部出力」フラグ**の粒度と保持場所（Dispatches 専用か汎用か／どのテーブル・設定か）。
- R4：Orders/Create「3=印刷+FAX」時、PDFエージェント（印刷）と SMTPエージェント（FAX）の**2系統同時投入**＝既存 dispatch/print 経路（二重生成回避）を踏襲で良いか。
- R5：ユーザーが PDFエージェント出力プリンタ未割当のときの挙動（サーバ既定へ／出力スキップ／エラー）。

### 再開合図（更新）
「再開します、session-memoを確認」。最新は本ファイル（20260714）。方式マトリクス確定。次＝R1〜R5 を詰める → CommonModule（プリンタ一覧I/F）→ MaterialModule（ユーザー割当・PDFプリント/PDFエージェント/SMTPエージェント・外部出力フラグ・テスト出力CB）で spec 起票。

---

## 🔴 本日のクローズ・チェックポイント（2026/07/14 終了）

### 本日の成果（印刷方式＝要件協議のみ・実装なし・spec未着手）
- 用語確定：**PDFプリント**（ローカル既定・ダイアログ・Web が HTTP 配信）／**PDFエージェント**（サーバ登録プリンタ・PrintAgent サイレント）／**SMTPエージェント**（メール/FAX送信）。
- PDF保管パス固定 `\\OJIADM23120073\app_share\PrintAgent\`＝両方式共用。PDFプリントは**HTTP配信**（UNC を渡さない）で問題なし。
- **方式マトリクス確定**（本ファイル上部の表）：
  - Orders/Create：承認後・出力区分判定（1=印刷→PDFエージェント〔ユーザー毎にプリンタ設定〕／2=FAX→SMTPエージェント／3=両方／0=なし）・ユーザー設定・テスト出力CB要。
  - Dispatches(i)：「請求」押下・PDFプリント・ユーザー設定・CB不要。
  - Dispatches(ii)：「請求」押下・PDFエージェント・**システム「外部出力」ON時のみ**・CB要。
  - Receivings：「入庫伝票」押下・PDFプリント・ユーザー設定・CB不要。
- **テスト出力＝エージェント利用時のみ**。CB ON で本番フロー（Approvals 等）をバイパス。PDFエージェント→自分の割当プリンタ／SMTPエージェント→自分宛メール。
- **ユーザーサイド テスト出力**（テスト印刷・テストSMTP）を可能にする（従来はシステム側のみ）。

### 設計モデル（確定寄り）
- PDFエージェント出力プリンタ＝**ユーザー毎割当**（user→サーバ登録プリンタ名）。割当マスタ＝MaterialModule 側（例 `m_user_print_setting`）。選択元一覧＝CommonModule から取得（read I/F 要）。
- サーバへのプリンタ登録＝システム/管理者。ユーザーは登録済みから選択のみ。
- 「外部出力」フラグ（システム設定）＝Dispatches(ii) 出力有無制御。

### 🟡 次回の最優先＝残確認 R1〜R5（未確定）
- **R1**：CommonModule に**プリンタ一覧 read I/F（`IPrinterQueryService` 等）追加**を許可するか（ユーザー割当の選択元に必須）。不可なら MaterialModule から db_common `m_printer` 読み取り専用参照。
- **R2**：ユーザー割当UI＝ユーザー自己サービス／管理者代理 のどちらか。
- **R3**：「外部出力」フラグの粒度・保持場所（Dispatches 専用/汎用・どのテーブル/設定）。
- **R4**：Orders/Create「3=印刷+FAX」＝PDFエージェント＋SMTPエージェント 2系統同時投入（既存経路・二重生成回避）踏襲で良いか。
- **R5**：PDFエージェント出力プリンタ未割当時の挙動（サーバ既定へ／スキップ／エラー）。

### 継続オープン（前出）
- 判別管理の User/System 優先順位の最終形（制御主体＝ユーザー設定/システム設定はページ×出力先単位で確定寄り）。
- テスト出力CB の搭載ページ（エージェント使用＝Orders/Create・Dispatches(ii)）。
- 未到達フォールバック（PrintAgent は指定プリンタ未インストール時 status=9 エラー・既定へFBせず＝要改修判断）。

### 想定 spec 分割（未作成）
- CommonModule spec：プリンタ一覧 read I/F 追加（R1）／必要ならテスト送信の再利用・PrintAgent 未到達FB改修（別repo）。
- MaterialModule spec：ユーザー割当マスタ＋UI／PDFプリント（HTTP配信）／PDFエージェント投入（printer_name 解決）／SMTPエージェント／外部出力フラグ／2カ所出力（Dispatches）／テスト出力CB。

### 状態（確定）
- **order-report-sender-info は実装完了・コミット済み**（MaterialModule `ab73774`／Nonaka `564aa61`）。push は各repoユーザー実施。
- 印刷方式は**要件協議段階**。コード変更・spec ファイルは**未着手**（本日はコミット対象の新規変更なし。memo 20260713/20260714 のみ更新）。

### 再開合図
「再開します、session-memoを確認」。最新は本ファイル（20260714）。次アクション＝R1（CommonModule read I/F 追加可否）から確定 → R2〜R5 → CommonModule→MaterialModule の順で spec 起票。
